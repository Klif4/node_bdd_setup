import {Given} from 'cucumber'
import {When} from 'cucumber'
import {Then} from 'cucumber'
import assert from "assert";

Given(/^I Have (\d+)$/, function (a) {
    assert.equal(true, true)

});
When(/^I add (\d+)$/, function (a) {

});
Then(/^I have (\d+)$/, function (a) {

});
